---
phase: 10-tiff-and-alto-data-endpoints
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_app.py
autonomous: true
requirements: [VIEW-02, VIEW-03]

must_haves:
  truths:
    - "GET /image/<stem> returns 200 JPEG for a TIFF in uploads/"
    - "GET /image/<stem> returns 404 JSON when stem is absent"
    - "GET /image/<stem> returns 400 JSON when stem contains path traversal characters"
    - "GET /alto/<stem> returns 200 JSON with page_width, page_height, jpeg_width, jpeg_height, and words array"
    - "GET /alto/<stem> returns 404 JSON when ALTO XML is absent"
    - "GET /alto/<stem> returns 400 JSON when stem contains path traversal characters"
    - "Words in ALTO response include id, content, hpos, vpos, width, height, confidence fields"
    - "Words with no WC attribute have confidence: null (not 0)"
  artifacts:
    - path: "tests/test_app.py"
      provides: "RED test suite for Phase 10 endpoints"
      contains: "test_image_endpoint, test_alto_endpoint"
  key_links:
    - from: "tests/test_app.py"
      to: "app.py GET /image/<stem>"
      via: "client.get('/image/<stem>')"
      pattern: "client\\.get.*image"
    - from: "tests/test_app.py"
      to: "app.py GET /alto/<stem>"
      via: "client.get('/alto/<stem>')"
      pattern: "client\\.get.*alto"
---

<objective>
Write a RED test suite for Phase 10's two new GET endpoints. Tests must fail with 404 (route not found) because neither endpoint exists yet in app.py. Every behavioral contract locked in CONTEXT.md becomes a failing test now, so Phase 10's implementation plan has a precise green target.

Purpose: Forces interface-first design; catches regressions when Phase 11 and Phase 12 touch app.py.
Output: tests/test_app.py extended with RED tests for GET /image/<stem> and GET /alto/<stem>
</objective>

<execution_context>
@/Users/zu54tav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zu54tav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-tiff-and-alto-data-endpoints/10-CONTEXT.md
@.planning/phases/10-tiff-and-alto-data-endpoints/10-RESEARCH.md
@tests/test_app.py
@tests/conftest.py
@app.py
</context>

<feature>
  <name>RED test suite: GET /image/<stem> and GET /alto/<stem></name>
  <files>tests/test_app.py</files>
  <behavior>
    All tests below must FAIL (404 route not found or AssertionError) when run before the implementation plan.

    --- GET /image/<stem> ---

    Path traversal rejection (400):
      - client.get('/image/../etc/passwd') → 400, JSON body contains 'error'
      - client.get('/image/folder/scan') → 400, JSON body contains 'error'

    Missing TIFF (404):
      - client.get('/image/nonexistent') → 404, body = {"error": "not found", "stem": "nonexistent"}

    Cache hit (200 JPEG served from jpegcache/):
      - Write a small synthetic JPEG file to tmp_path/jpegcache/scan_001.jpg
      - client.get('/image/scan_001') → 200, Content-Type = image/jpeg

    TIFF render path (200 JPEG rendered from TIFF):
      - Write a 4x4 pixel minimal TIFF to tmp_path/uploads/scan_001.tif using Pillow
      - client.get('/image/scan_001') → 200, Content-Type = image/jpeg
      - jpegcache/scan_001.jpg exists after the request (cache was written)

    Filename case mismatch (server restart scenario):
      - Write tmp_path/uploads/Scan_002.tif (original case)
      - Request client.get('/image/scan_002') (lowercase stem)
      - → 200 (endpoint finds the file by scanning uploads/)

    --- GET /alto/<stem> ---

    Path traversal rejection (400):
      - client.get('/alto/../etc/passwd') → 400, JSON body contains 'error'

    Missing ALTO XML (404):
      - client.get('/alto/nonexistent') → 404, body = {"error": "not found", "stem": "nonexistent"}

    Valid ALTO XML (200 JSON):
      - Write a minimal valid ALTO 2.1 XML file to tmp_path/alto/scan_003.xml with:
          namespace http://schema.ccs-gmbh.com/ALTO
          Page element with WIDTH="5146" HEIGHT="7548"
          Two String elements: CONTENT="Hallo" HPOS="100" VPOS="200" WIDTH="80" HEIGHT="30" WC="0.95"
                               CONTENT="Welt" HPOS="200" VPOS="200" WIDTH="60" HEIGHT="30" (no WC attribute)
      - client.get('/alto/scan_003') → 200
      - Response JSON contains: page_width=5146, page_height=7548
      - Response JSON contains: words array of length 2
      - First word: id="w0", content="Hallo", hpos=100, vpos=200, width=80, height=30, confidence=0.95
      - Second word: id="w1", content="Welt", confidence=null (not 0, not absent)
      - Response JSON contains: jpeg_width (int >= 0), jpeg_height (int >= 0)

    JPEG dimensions when cache exists:
      - Write tmp_path/jpegcache/scan_003.jpg (any valid JPEG, e.g. 960x1600 Pillow save)
      - client.get('/alto/scan_003') → jpeg_width=960, jpeg_height=1600

    JPEG dimensions computed from TIFF when cache absent:
      - Write tmp_path/uploads/scan_004.tif (4x8 pixel TIFF — longest side = 8, under 1600, no scaling)
      - Write tmp_path/alto/scan_004.xml (same structure as scan_003.xml with dummy Page dimensions)
      - No jpegcache/scan_004.jpg
      - client.get('/alto/scan_004') → jpeg_width=4, jpeg_height=8 (no scaling because 8 < 1600)

    Malformed ALTO XML (500):
      - Write tmp_path/alto/corrupt.xml containing "not xml at all"
      - client.get('/alto/corrupt') → 500, JSON body contains 'error'
  </behavior>
  <implementation>
    Append new test classes/functions to tests/test_app.py after the existing Phase 9 tests.
    Use the existing `client` and `flask_app` fixtures from conftest.py.
    Use `tmp_path` (available via flask_app fixture which already uses it) — access via flask_app.config['OUTPUT_DIR'].

    Helper pattern for ALTO XML fixture:
    ```python
    ALTO_NS = 'http://schema.ccs-gmbh.com/ALTO'

    def _write_alto(path, page_w, page_h, strings):
        """Write minimal ALTO 2.1 XML. strings = list of dicts with CONTENT/HPOS/VPOS/WIDTH/HEIGHT and optional WC."""
        from lxml import etree
        ns = ALTO_NS
        root = etree.Element(f'{{{ns}}}alto')
        layout = etree.SubElement(root, f'{{{ns}}}Layout')
        page = etree.SubElement(layout, f'{{{ns}}}Page')
        page.set('WIDTH', str(page_w))
        page.set('HEIGHT', str(page_h))
        block = etree.SubElement(page, f'{{{ns}}}PrintSpace')
        tb = etree.SubElement(block, f'{{{ns}}}TextBlock')
        line = etree.SubElement(tb, f'{{{ns}}}TextLine')
        for s in strings:
            elem = etree.SubElement(line, f'{{{ns}}}String')
            for k, v in s.items():
                elem.set(k, str(v))
        path.write_bytes(etree.tostring(root, xml_declaration=True, encoding='UTF-8', pretty_print=True))
    ```

    Helper for minimal TIFF:
    ```python
    def _write_tiff(path, w=4, h=4, mode='RGB'):
        from PIL import Image
        img = Image.new(mode, (w, h), color=(128, 128, 128))
        img.save(str(path), format='TIFF')
    ```

    RED confirmation: run `pytest tests/test_app.py -k "image or alto" -x --tb=short` and verify new tests fail with 404 (not with syntax error or ImportError).
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Write RED tests for GET /image/<stem></name>
  <files>tests/test_app.py</files>
  <action>
    Append a new test class `TestImageEndpoint` to tests/test_app.py after all existing Phase 9 tests.

    Include these test methods (do NOT implement any app routes yet):
    - test_path_traversal_dot_dot: GET /image/../etc/passwd → 400, response JSON has 'error' key
    - test_path_traversal_slash: GET /image/folder/scan → 400 (note: Flask may normalize this URL; if Flask returns 301/302, also accept that as a non-200 non-JPEG response — document in test)
    - test_missing_tiff_returns_404: GET /image/nonexistent → 404, JSON = {"error": "not found", "stem": "nonexistent"}
    - test_cache_hit_serves_jpeg: write a valid 1x1 JPEG to output_dir/jpegcache/scan_001.jpg, then GET /image/scan_001 → 200, Content-Type contains 'jpeg'
    - test_tiff_render_writes_cache: write a 4x4 RGB TIFF to output_dir/uploads/scan_001.tif, GET /image/scan_001 → 200, and verify output_dir/jpegcache/scan_001.jpg exists afterwards
    - test_filename_case_mismatch: write output_dir/uploads/Scan_002.tif, GET /image/scan_002 → 200 (endpoint resolves original filename via directory scan)

    Use `flask_app` fixture to get `output_dir = Path(flask_app.config['OUTPUT_DIR'])`. Create subdirectories as needed with `mkdir(parents=True, exist_ok=True)`.

    Helper `_write_tiff(path, w=4, h=4)` defined at module level (see feature implementation block).

    Do NOT touch any existing test in the file. Preserve all Phase 9 tests exactly.
  </action>
  <verify>
    <automated>cd /Users/zu54tav/Zeitschriften-OCR && python -m pytest tests/test_app.py::TestImageEndpoint -x --tb=short 2>&1 | tail -20</automated>
    <manual>Confirm tests FAIL (not error). Expected: "FAILED tests/test_app.py::TestImageEndpoint::test_*" with 404 Not Found — not ImportError or SyntaxError. At least 5 failures from the 6 image tests.</manual>
    <sampling_rate>run after Task 1 commits, before Task 2 begins</sampling_rate>
  </verify>
  <done>TestImageEndpoint class exists in tests/test_app.py with 6 test methods, all FAILING with route-not-found (404) or assertion errors — not syntax/import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED tests for GET /alto/<stem></name>
  <files>tests/test_app.py</files>
  <action>
    Append a new test class `TestAltoEndpoint` to tests/test_app.py after `TestImageEndpoint`.

    Define the `_write_alto(path, page_w, page_h, strings)` helper at module level (see feature implementation block above).

    Include these test methods:
    - test_path_traversal_rejected: GET /alto/../etc → 400, JSON has 'error' key
    - test_missing_alto_returns_404: GET /alto/nonexistent → 404, JSON = {"error": "not found", "stem": "nonexistent"}
    - test_valid_alto_returns_json_shape: write scan_003.xml with Page WIDTH=5146 HEIGHT=7548 and two String elements (first with WC, second without); GET /alto/scan_003 → 200; assert response JSON has keys page_width=5146, page_height=7548, jpeg_width (int), jpeg_height (int), words (list of 2)
    - test_word_fields_correct: using same scan_003.xml; first word id="w0", content="Hallo", hpos=100, vpos=200, width=80, height=30, confidence=0.95; second word confidence is None (not 0)
    - test_jpeg_dims_from_cache: write scan_003.jpg (4x8 JPEG via Pillow) in jpegcache/; GET /alto/scan_003 → jpeg_width=4, jpeg_height=8
    - test_jpeg_dims_computed_from_tiff_when_cache_absent: write scan_004.tif (4x8 pixels, no scaling needed since 8 < 1600) and scan_004.xml; no jpegcache entry; GET /alto/scan_004 → jpeg_width=4, jpeg_height=8
    - test_malformed_xml_returns_500: write corrupt.xml = b"not xml at all"; GET /alto/corrupt → 500, JSON has 'error' key

    Two String elements for scan_003 fixture:
      [{"CONTENT": "Hallo", "HPOS": "100", "VPOS": "200", "WIDTH": "80", "HEIGHT": "30", "WC": "0.95"},
       {"CONTENT": "Welt", "HPOS": "200", "VPOS": "200", "WIDTH": "60", "HEIGHT": "30"}]

    For jpeg dims from TIFF test: write 4x8 TIFF to uploads/scan_004.tif using `_write_tiff(path, w=4, h=8)`. The endpoint must compute jpeg_width=4, jpeg_height=8 (no scaling, since max(4,8)=8 < 1600).

    Commit: `test(10-01): add RED tests for GET /image and GET /alto endpoints`
  </action>
  <verify>
    <automated>cd /Users/zu54tav/Zeitschriften-OCR && python -m pytest tests/test_app.py::TestAltoEndpoint -x --tb=short 2>&1 | tail -20</automated>
    <manual>Confirm all 7 alto tests FAIL with route-not-found (404) or similar — not SyntaxError or ImportError. Full suite (28 existing + 13 new) should show exactly the 13 new tests failing.</manual>
    <sampling_rate>run after Task 2 commits before Phase 10 Plan 02 begins</sampling_rate>
  </verify>
  <done>TestAltoEndpoint class exists with 7 test methods all FAILING. Full test run shows 28 passing (existing Phase 1-9 tests) + 13 failing (new Phase 10 tests). Zero syntax/import errors.</done>
</task>

</tasks>

<verification>
After both tasks:
```bash
cd /Users/zu54tav/Zeitschriften-OCR
python -m pytest tests/test_app.py --tb=line 2>&1 | tail -30
```
Expected: 28 passed, 13 failed. No errors. Failure reason is 404 (route not found) — not SyntaxError or ImportError.
</verification>

<success_criteria>
- tests/test_app.py has 13 new test methods across TestImageEndpoint (6) and TestAltoEndpoint (7)
- All 13 new tests FAIL with route-not-found responses (not code errors)
- All 28 existing tests still PASS (zero regressions)
- Helper functions _write_tiff and _write_alto defined at module level for reuse in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/10-tiff-and-alto-data-endpoints/10-01-SUMMARY.md`
</output>
