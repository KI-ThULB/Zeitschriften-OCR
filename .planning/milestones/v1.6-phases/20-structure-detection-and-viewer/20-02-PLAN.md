---
phase: 20-structure-detection-and-viewer
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - templates/viewer.html
autonomous: false
requirements:
  - VIEW-07

must_haves:
  truths:
    - "Text panel renders paragraph blocks with visible blank-line gaps between them, not a flat word stream"
    - "Headings render bold and slightly larger (1.1em); captions and advertisements render italic and smaller (0.9em); advertisements have a left border"
    - "Role count summary (e.g. '1 heading, 3 paragraphs') appears below the confidence badge and updates on every file load"
    - "Structural role labels update correctly when user navigates to a different file via the sidebar"
    - "Confidence styling (faded low-confidence words) still works after structured rendering is active"
    - "Word click-to-edit, SVG highlight, and applyConfidenceStyling all function correctly after renderBlocks() replaces renderWords()"
  artifacts:
    - path: "templates/viewer.html"
      provides: "renderBlocks() replacing renderWords(); updateStructSummary(); CSS role styling; #struct-summary HTML"
      contains: "function renderBlocks"
    - path: "templates/viewer.html"
      provides: "CSS .para-block and [data-role=...] rules"
      contains: "para-block"
  key_links:
    - from: "loadFile()"
      to: "renderBlocks(paraBlocks)"
      via: "called after buildParaBlocks() with full paraBlocks result"
      pattern: "renderBlocks\\(paraBlocks\\)"
    - from: "loadArticles()"
      to: "renderBlocks(paraBlocks)"
      via: "re-render after VLM data resolves (currentBlocks guard)"
      pattern: "renderBlocks\\(paraBlocks\\)"
    - from: "renderBlocks()"
      to: "#word-list"
      via: "document.getElementById('word-list').innerHTML = html"
      pattern: "getElementById\\('word-list'\\)\\.innerHTML"
    - from: "#struct-summary"
      to: "updateStructSummary()"
      via: "called after every renderBlocks() call"
      pattern: "updateStructSummary"
---

<objective>
Replace `renderWords()` with `renderBlocks()` in viewer.html, add `updateStructSummary()` and CSS role styling, and wire everything into `loadFile()` and `loadArticles()`. This delivers VIEW-07: the structured text panel with visually differentiated headings, paragraphs, captions, and advertisements.

Purpose: Users see text organized into labeled paragraphs and headings rather than a flat word list. VLM-assigned roles manifest visually — bold headings, italic captions, bordered advertisements — so article structure is readable without consulting the TIFF image.

Output: `renderBlocks()`, `updateStructSummary()`, CSS rules for `.para-block` and `[data-role=...]`, `#struct-summary` HTML element, and all `loadFile()`/`loadArticles()` integration wiring.
</objective>

<execution_context>
@/Users/zu54tav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zu54tav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-structure-detection-and-viewer/20-CONTEXT.md
@.planning/phases/20-structure-detection-and-viewer/20-RESEARCH.md
@.planning/phases/20-structure-detection-and-viewer/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add renderBlocks(), updateStructSummary(), CSS role styling, and #struct-summary HTML</name>
  <files>templates/viewer.html</files>
  <action>
Three additions to viewer.html:

**A. CSS in `<style>` block** — add after existing `.word`, `.word-input` rules:

```css
.para-block {
  margin-bottom: 0.75em;
}
.para-block[data-role="heading"] {
  font-weight: bold;
  font-size: 1.1em;
}
.para-block[data-role="caption"] {
  font-size: 0.9em;
  font-style: italic;
}
.para-block[data-role="advertisement"] {
  font-size: 0.9em;
  font-style: italic;
  border-left: 3px solid #ccc;
  padding-left: 0.4em;
}
```

No additional CSS for `[data-role="paragraph"]` — it uses default styling.

**B. `renderBlocks(paraBlocks)` function** — replaces `renderWords()` as the primary renderer:
- Target: `document.getElementById('word-list')` (NOT `#text-panel` — invariant from Phase 19)
- Empty guard: if `paraBlocks.length === 0` OR every block has `words.length === 0`, set `list.innerHTML = '<em>No words found in this file.</em>'` and return
- Build HTML: for each `paraBlock` in `paraBlocks`, emit `<div class="para-block" data-role="${escapeHtml(pb.role)}">` containing word spans: `<span class="word" data-id="${escapeHtml(w.id)}" data-wc="${w.confidence ?? ''}">${escapeHtml(w.content)}</span> ` (space after each word, same as `renderWords()`)
- Use `escapeHtml()` already in scope from Phase 12
- After setting `list.innerHTML`, re-attach the word click handler: `list.onclick = wordListClickHandler` — preserve the delegated click handler from Phase 12 (do NOT break click-to-edit)
- NOTE: `renderBlocks()` writes `data-wc` on each span exactly as `renderWords()` did; this ensures `applyConfidenceStyling()` still finds `.word[data-wc]` spans

**C. `updateStructSummary(paraBlocks)` function:**
- Count roles across paraBlocks where `words.length > 0` (skip empty blocks)
- Order in summary string: heading, paragraph, caption, advertisement
- Format: `"1 heading, 3 paragraphs, 1 caption"` — pluralise correctly (heading/headings, paragraph/paragraphs, caption/captions, ad/ads)
- Target: `document.getElementById('struct-summary')` — set `.textContent = parts.join(', ')` or empty string if no counts
- If element not found (HTML not yet added), silently return — defensive guard

**D. `#struct-summary` HTML element** — add inside `#wc-settings` div, immediately after `#wc-badge`:
```html
<div id="struct-summary" style="font-size:0.78rem;color:#888;margin-top:2px;min-height:1em;"></div>
```
This element persists across file loads because it is inside `#wc-settings` (which is a sibling of `#word-list`, not a child — the Phase 19 inner container pattern keeps it safe).
  </action>
  <verify>
    <automated>cd /Users/zu54tav/Zeitschriften-OCR && python -m pytest tests/ -q 2>&amp;1 | tail -5</automated>
    <manual>Verify CSS is in the style block: `grep -n "para-block" templates/viewer.html`. Verify functions exist: `grep -n "function renderBlocks\|function updateStructSummary" templates/viewer.html`. Verify struct-summary HTML: `grep -n "struct-summary" templates/viewer.html`.</manual>
  </verify>
  <done>`renderBlocks()`, `updateStructSummary()`, CSS `.para-block`/`[data-role=...]` rules, and `#struct-summary` HTML all present in viewer.html; 136 pytest tests still green</done>
</task>

<task type="auto">
  <name>Task 2: Replace renderWords() calls with renderBlocks() in loadFile() and loadArticles()</name>
  <files>templates/viewer.html</files>
  <action>
Replace the rendering call sites in `loadFile()` and `loadArticles()`:

**In `loadFile()`:**
1. After `displayWords = normalizeWords(data.words, data.blocks || [], data.page_width)` and after `currentBlocks = data.blocks || []` (from Plan 01 Task 2):
2. Build `paraBlocks`:
   ```javascript
   const paraBlocks = buildParaBlocks(currentBlocks, wordById, currentArticles, pageWidth, pageHeight);
   ```
3. Replace the `renderWords(displayWords)` call with:
   ```javascript
   renderBlocks(paraBlocks);
   updateStructSummary(paraBlocks);
   ```
4. Keep `applyConfidenceStyling(wcThreshold)` AFTER `renderBlocks()` — order matters (Research pitfall 5: calling before spans exist finds nothing)
5. `renderWords()` function itself remains in the codebase (do not delete) — it may be used by tests or future code. Only remove its call site in `loadFile()`.

**In `loadArticles(stem)`:**
Replace the Plan 01 placeholder stub (the `// Plan 02 will call renderBlocks...` comment block) with real calls:
```javascript
if (currentBlocks && currentBlocks.length > 0) {
  const paraBlocks = buildParaBlocks(currentBlocks, wordById, currentArticles, pageWidth, pageHeight);
  renderBlocks(paraBlocks);
  updateStructSummary(paraBlocks);
  applyConfidenceStyling(wcThreshold);
}
```
This is the load-sequencing fix for Research pitfall 2: `loadArticles()` is async and may resolve after `loadFile()` has already rendered with empty `currentArticles`. The re-render here gives proper VLM role assignment once the data arrives.

**Critical invariants to preserve:**
- `wordById` must NOT be changed — it maps original `data.words` IDs for click-to-edit
- `displayWords` and `normalizeWords()` remain unchanged — used for the edit pipeline
- Write only to `#word-list` — do not touch `#wc-settings` or its children except via `updateStructSummary()`
- `applyConfidenceStyling(wcThreshold)` must be called AFTER `renderBlocks()` at every call site
- The generation guard (`if (gen !== loadGen) return`) in `loadFile()` must NOT be removed

**Deferred features (from CONTEXT.md) — do NOT implement:**
- Flat/structured toggle button
- Heuristic heading detection for pages without VLM
- "Segment this page" prompt
  </action>
  <verify>
    <automated>cd /Users/zu54tav/Zeitschriften-OCR && python -m pytest tests/ -q 2>&amp;1 | tail -5</automated>
    <manual>
1. Start Flask: `python app.py` (or `flask run`)
2. Open viewer at http://localhost:5000
3. Load a TIFF file with known VLM segmentation (from output/segments/)
4. Verify: text panel shows `.para-block` divs with blank line gaps between them (browser inspector: Elements tab)
5. Verify: heading blocks appear bold and slightly larger
6. Verify: `#struct-summary` shows a role count string (e.g. "1 heading, 3 paragraphs")
7. Click a word — edit input should open (click-to-edit still works)
8. Set confidence threshold slider to 0.9 — low-confidence words should fade (applyConfidenceStyling works)
9. Navigate to a second file in sidebar — role labels should update correctly
10. Load a file without VLM data — text panel shows paragraphs (no heading styling), struct-summary shows only paragraph counts
    </manual>
  </verify>
  <done>
- `renderBlocks(paraBlocks)` is called in `loadFile()` replacing `renderWords(displayWords)`
- `renderBlocks(paraBlocks)` and `updateStructSummary(paraBlocks)` both called in `loadArticles()` re-render block
- `applyConfidenceStyling(wcThreshold)` called after each `renderBlocks()` invocation
- Text panel shows structured blocks with CSS role styling
- Role count summary visible in right panel below confidence badge
- All word-level features (click-to-edit, confidence styling, SVG highlight) remain functional
- All 136 pytest tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify structured text panel visually and functionally</name>
  <action>Human verification of the complete structured text rendering — visual inspection of role styling, paragraph gaps, and role count summary, plus functional verification of preserved word-level features.</action>
  <what-built>Structured text panel with paragraph detection, VLM role styling, and role count summary. Headings are bold/larger, captions italic/smaller, advertisements italic/smaller with left border, paragraphs use default styling. Role count summary displays below confidence badge. Re-render after VLM data loads ensures correct roles even on async timing paths.</what-built>
  <how-to-verify>
1. Start the Flask server: `cd /Users/zu54tav/Zeitschriften-OCR && python app.py`
2. Open http://localhost:5000 and load a file with multiple TextBlocks
3. Check text panel: blocks should be visually separated (blank line gaps), not a flat run-on word list
4. If the file has VLM segmentation in output/segments/, headings should appear bold
5. Check the right panel below the confidence badge — role count should show (e.g. "2 headings, 5 paragraphs")
6. Open a second file — role labels should refresh correctly
7. Set confidence slider to 0.9 — low-confidence words should fade
8. Click a word — edit flow should open (Enter to save, Escape to cancel)
9. Zoom in/out with mouse wheel — block layout should stay intact
  </how-to-verify>
  <verify>
    <automated>cd /Users/zu54tav/Zeitschriften-OCR && python -m pytest tests/ -q 2>&amp;1 | tail -3</automated>
  </verify>
  <done>User confirms structured panel renders correctly with visual role styling and all existing features (click-to-edit, confidence slider, zoom) remain functional</done>
  <resume-signal>Type "approved" if the structured panel looks correct, or describe any visual or functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
Run full test suite: `cd /Users/zu54tav/Zeitschriften-OCR && python -m pytest tests/ -q`

Expected: 136 tests collected, 136 passed, 0 failed.

Grep checks:
- `grep -n "function renderBlocks" templates/viewer.html` — 1 result
- `grep -n "renderBlocks(paraBlocks)" templates/viewer.html` — at least 2 results (loadFile + loadArticles)
- `grep -n "updateStructSummary" templates/viewer.html` — at least 3 results (definition + 2 call sites)
- `grep -n "para-block" templates/viewer.html` — results in both CSS and JS (renderBlocks template string)
- `grep -n "struct-summary" templates/viewer.html` — results in both HTML and updateStructSummary()
- `grep -n "renderWords(displayWords)" templates/viewer.html` — 0 results (call site replaced; function definition still present is OK)
</verification>

<success_criteria>
- Text panel renders `.para-block[data-role=...]` divs with margin-bottom gaps between them — no flat word stream
- Headings: `font-weight: bold`, `font-size: 1.1em`
- Captions: `font-size: 0.9em`, `font-style: italic`
- Advertisements: `font-size: 0.9em`, `font-style: italic`, `border-left: 3px solid #ccc`, `padding-left: 0.4em`
- Role count summary renders in `#struct-summary` below confidence badge
- Summary updates on file load and on VLM data arrival (two render passes)
- All existing features preserved: click-to-edit, confidence slider, SVG highlight, zoom/pan
- Navigation to a different file correctly refreshes role labels and summary
- All 136 pytest tests pass
- No flat/structured toggle (deferred to future phase per CONTEXT.md)
</success_criteria>

<output>
After completion, create `.planning/phases/20-structure-detection-and-viewer/20-02-SUMMARY.md`
</output>
